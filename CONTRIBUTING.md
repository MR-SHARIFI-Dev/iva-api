# مشارکت در IVA API

از مشارکت شما استقبال می‌شود.

1. پروژه را Fork کنید و یک Branch جدا بسازید.
2. تغییرات را با PHP 8.1 یا جدیدتر آزمایش کنید.
3. خروجی همه endpointها باید JSON و سازگار با ساختار فعلی باقی بماند.
4. هیچ توکن خصوصی، رمز، کوکی یا اطلاعات شخصی Commit نکنید.
5. برای providerهای خارجی timeout و مدیریت خطای استاندارد در نظر بگیرید.
6. Pull Request را همراه توضیح تغییر و نمونه پاسخ باز کنید.

برای بررسی سریع نحو:

```bash
php -l api.php
```
